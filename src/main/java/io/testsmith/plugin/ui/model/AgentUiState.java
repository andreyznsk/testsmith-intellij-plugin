package io.testsmith.plugin.ui.model;

public enum AgentUiState {
    IDLE,
    RUNNING,
    ANALYZING,
    GENERATING,
    VERIFYING,
    FIXING,
    COVERAGE_UPDATE,
    WAITING_APPROVAL,
    COMPLETED,
    STOPPED,
    ERROR,
    STOPPING
}
