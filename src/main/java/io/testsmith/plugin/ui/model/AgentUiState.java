package io.testsmith.plugin.ui.model;

public enum AgentUiState {
    IDLE,
    ANALYZING,
    GENERATING,
    VERIFYING_TARGET,
    RUNNING_FULL_SUITE,
    WAITING_FOR_APPROVAL,
    STOPPED,
    ERROR
}
